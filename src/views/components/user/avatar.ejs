<svg
  class="avatar"
  data-jdenticon-value="<%= user._id %>"
  width="<%= size %>"
  height="<%= size %>"
>
  <image
    src="/static/img/favicon.png"
    alt="avatar"
    width="<%= size %>"
    height="<%= size %>"
  />
</svg>
